# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rift
DB_USER=rift
DB_PASSWORD=123

# App
ENVIRONMENT=local   # local | staging | production

# Admin seed (created on startup if not exists)
ADMIN_EMAIL=admin@rift.dev
ADMIN_PASSWORD=admin123

# JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_ALG=HS256
JWT_EXP=30   # minutes
